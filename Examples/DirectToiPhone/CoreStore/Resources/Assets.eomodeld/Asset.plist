{
    attributes = (
        {
            columnName = "ASSET_ID"; 
            externalType = NUMBER; 
            name = assetID; 
            valueClassName = NSNumber; 
            valueType = d; 
        }, 
        {
            columnName = TOKEN; 
            externalType = VARCHAR2; 
            name = token; 
            valueClassName = NSString; 
            valueType = S; 
            width = 255; 
        }, 
        {
            columnName = "ENTITY_NAME"; 
            externalType = VARCHAR2; 
            name = entityName; 
            valueClassName = NSString; 
            valueType = S; 
            width = 50; 
        }
    ); 
    attributesUsedForLocking = (assetID, token, entityName); 
    className = EOGenericRecord; 
    classProperties = (token, entityName); 
    externalName = ASSET; 
    fetchSpecificationDictionary = {}; 
    internalInfo = {"_clientClassPropertyNames" = (token, entityName); uniqueID = 203828598; }; 
    isAbstractEntity = Y; 
    name = Asset; 
    primaryKeyAttributes = (assetID); 
    userInfo = {fetchSpecificationDictionary = {}; }; 
}